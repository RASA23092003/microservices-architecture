spring:
  application:
    name: EPharmacy-OrderMS
  datasource:
    # url: jdbc:mysql://mysql:3306/EPharmacy_OrderMS_DB?useSSL=false&createDatabaseIfNotExist=true
    url: jdbc:mysql://localhost:3306/EPharmacy_OrderMS_DB?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  cloud:
    consul:
      # host: consul
      port: 8500
      discovery:
        enabled: true
        register: true
        service-name: ${spring.application.name}

server:
  port: 6500
  servlet:
    context-path: /epharmacy